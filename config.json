{
  "name": "TextEditor",
  "viewName": "Text Editor",
  "type": "executer",
  "main": "app.js",
  "execProgram": "node",
  "version": "1.3.0",
  "description": "basic create, read, edit, search functions for text files",
  "imgUrl": "logo.png",
  "category": [
    "FILES",
    "CORE"
  ],
  "methods": [
    {
      "name": "createFile",
      "viewName": "Create New File",
      "params": [
        {
          "name": "PATH",
          "viewName": "File Path",
          "type": "string",
          "required": true,
          "description": "The complete path and filename for the new file",
          "placeholder": "/home/logadmin/scripts/export.sh"
        },
        {
          "name": "CONTENT",
          "viewName": "File Content",
          "type": "text",
          "required": true,
          "description": "The initial contents of the file",
          "placeholder": "#!/bin/bash\ntar -czvf logs-$(date +%F).tgz /var/log/*.log"
        },
        {
          "name": "overwrite",
          "viewName": "Overwrite Existing",
          "description": "If selected, any existing file at the File Path will be overwritten.",
          "type": "boolean",
          "default": true
        }
      ]
    },
    {
      "name": "createVaultFile",
      "viewName": "Create File From Vault",
      "params": [
        {
          "name": "PATH",
          "viewName": "File Path",
          "type": "string",
          "required": true,
          "description": "The complete path and filename for the new file",
          "placeholder": "/home/logadmin/scripts/export.sh"
        },
        {
          "name": "CONTENT",
          "viewName": "File Content",
          "type": "vault",
          "required": true,
          "description": "The Kaholo vault item continaing the contents for the file"
        }
      ]
    },
    {
      "name": "appendToFile",
      "viewName": "Append To File",
      "params": [
        {
          "name": "PATH",
          "viewName": "File Path",
          "type": "string",
          "required": true,
          "description": "The complete path and filename for file to be appended",
          "placeholder": "/home/logadmin/scripts/export.sh"
        },
        {
          "name": "CONTENT",
          "viewName": "Content",
          "type": "text",
          "required": true,
          "description": "The text to append to the file",
          "placeholder": "chmod 400 /home/logadmin/key001.pem"
        },
        {
          "name": "dontCreate",
          "viewName": "Don't Create",
          "type": "boolean",
          "description": "If selected, append only if file at File Path already exists"
        },
        {
          "name": "return",
          "viewName": "Return New Content",
          "type": "boolean",
          "description": "If selected, after appending the entire contents of the file are returned to Final Result."
        }
      ]
    },
    {
      "name": "searchInFile",
      "viewName": "Search In File",
      "params": [
        {
          "name": "PATH",
          "viewName": "File Path",
          "type": "string",
          "required": true,
          "description": "The complete path and filename for file to search",
          "placeholder": "/home/logadmin/scripts/export.sh"
        },
        {
          "name": "REGEX",
          "viewName": "Regexp",
          "type": "string",
          "description": "Regular expressions are patterns used to match character combinations in strings.",
          "placeholder": "^.*ERROR.*$"
        },
        {
          "name": "return",
          "viewName": "Return Matches",
          "type": "boolean",
          "default": false,
          "description": "If selected, all matching strings are returned. Otherwise only whether or not a match was found."
        }
      ]
    },
    {
      "name": "replaceText",
      "viewName": "Replace Text In File",
      "params": [
        {
          "name": "PATH",
          "viewName": "File Path",
          "type": "string",
          "required": true,
          "description": "The complete path and filename for the file to have replacements made",
          "placeholder": "/home/logadmin/scripts/export.sh"
        },
        {
          "name": "REGEX",
          "viewName": "Search Regex Expression",
          "type": "string",
          "required": true,
          "description": "Regular expressions are patterns used to match character combinations in strings.",
          "placeholder": "[eE][rR][rR][oO][rR]"
        },
        {
          "name": "REPLACE",
          "viewName": "Replace Value",
          "type": "string",
          "required": true,
          "description": "The string to replace those matching the Search Regex Expression",
          "placeholder": "Error"
        },
        {
          "name": "return",
          "viewName": "Return New Content",
          "type": "boolean",
          "default": false,
          "description": "If selected, after replacing the entire contents of the file are returned to Final Result."
        }
      ]
    },
    {
      "name": "getFileContent",
      "viewName": "Read File Content",
      "params": [
        {
          "name": "PATH",
          "viewName": "File Path",
          "type": "string",
          "required": true,
          "description": "The complete path and filename for the file to read",
          "placeholder": "/home/logadmin/scripts/export.sh"
        }
      ]
    }
  ]
}